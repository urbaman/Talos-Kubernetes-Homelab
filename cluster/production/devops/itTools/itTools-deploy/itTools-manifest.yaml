apiVersion: v1
kind: Namespace
metadata:
  name: it-tools
---
apiVersion: v1
kind: Service
metadata:
  name: it-tools
  namespace: it-tools
  labels:
    app: it-tools
spec:
  type: ClusterIP
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
  ports:
  - name: http
    port: 80
  selector:
    app: it-tools
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: it-tools
  namespace: it-tools
  labels:
    app: it-tools        
    app.kubernetes.io/name: it-tools
spec:
  replicas: 1
  selector:
    matchLabels:
      app: it-tools
  template:
    metadata:
      labels:
        app: it-tools        
        app.kubernetes.io/name: it-tools
    spec:
      containers:
      - image: corentinth/it-tools:latest
        imagePullPolicy: Always
        name: it-tools
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /
            port: 80
          failureThreshold: 3
          initialDelaySeconds: 10
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-urbaman-cloud
  namespace: it-tools
spec:
  # Certificate will be valid for these domain names
  dnsNames:
  - it-tools.urbaman.cloud
  # Reference our issuer
  # As it's a ClusterIssuer, it can be in a different namespace
  issuerRef:
    kind: ClusterIssuer
    name: cert-manager-acme-issuer
  # Secret that will be created with our certificate and private keys
  secretName: it-tools-urbaman-cloud
---
apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: traefik-it-tools-transport
  namespace: it-tools
spec:
  serverName: it-tools 
  insecureSkipVerify: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-it-tools-websecure
  namespace: it-tools
  annotations:
    # For Homepage service discovery
    gethomepage.dev/href: "https://it-tools.urbaman.cloud"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/description: "It Tools"
    gethomepage.dev/group: DevOps
    gethomepage.dev/icon: it-tools.svg
    gethomepage.dev/app: it-tools # optional, may be needed if app.kubernetes.io/name != ingressRoute metadata.name
    gethomepage.dev/name: "It Tools"
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`it-tools.urbaman.cloud`)
      services:
        - name: it-tools
          port: 80
          serversTransport: traefik-it-tools-transport
      middlewares:
        - name: security
          namespace: traefik
  tls:
    secretName: it-tools-urbaman-cloud
    options:
      name: tlsoptions
      namespace: traefik
